{
    "resource": [
        {
            "null_resource": [
                {
                    "app1": [
                        {
                            "triggers": {
                                "name": "${var.name}",
                                "network": "${var.network}"
                            },
                            "provisioner": [
                                {
                                    "local-exec": {
                                        "command": "echo 'Arrancando servidor ${var.name} en red ${var.network} puerto ${var.port} con API_KEY=${var.api_key}'"
                                    }
                                }
                            ]
                        }
                    ]
                }
            ]
        }
    ]
}